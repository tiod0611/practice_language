exam <- read.csv('../Data/csv_exam.csv')
exam
head(exam)
tail()
tail(exam)
head(exam, 10)
tail(exam, 10)
View(exam)
source("~/.active-rstudio-document", echo=TRUE)
dim(exam)
# 속성 파악하기
str(exam)
# 데이터 요약
summary(exam)
mpg = as.data.frame(ggplot::mpg)
mpg
mpg = as.data.frame(ggplot2::mpg)
mpg
head(mpg)
view(mpg)
View(mpg)
dim(mpg)
str(mpg)
summary(mpg)
?mpg
# column name 변경하기
df_raw = data.frame(var1 = c(1, 2, 3),
var2 = c(2, 3, 2))
df_raw
#dplyr 설치하고 가져오기
install.packages('dplyr')
library(dplyr)
df_new = df_raw
df_new
View(df_raw)
View(df_new)
View(exam)
# column 명 변경
df_new = rename(df_new, v2= var2) # var2라는 column을 v2로 변경경
df_new
# 연습문제 mpg 속 column명을 바꿔보자
mpg_new = mpg
View(mpg)
mpg_new = rename(mpg_new, city=cty, highway=hwy)
head(mpg_new)
df = data.frame(var1 = c(4, 3, 8),
var2 = c(2, 6, 1))
df
# 변수를 추가하자~
df$var_sum = df$var1 + df$var2
df
df$var_mean = (df$var_sum)/2
df
source("~/.active-rstudio-document", echo=TRUE)
mpg
head(mpg)
# 1. 기준값 정하기
summary(mpg)
hist(mpg$total)
# 조건에 따른 값을 새로운 변수로 만들기
mpg$test = ifelse(mpg$total >= 20, "pass", "fail")
head(mpg$test)
head(mpg)
# 빈도수 확인
table(mpg$test)
library(ggplot2)
qplot(mpg$test)
#연습문제 중첩 조건문을 활용하기
mpg$grade = ifelse(mpg$total >29, "A",
ifelse(mpg$total >19, "B", "C"))
head(mpg)
table(mpg$grade)
qplot(mpg$grade)
midwest
midwest=midwest
midwest=as.data.frame(ggplot2::midwest)
head(midwest)
dim(midwest)
str(midwest)
summary(midwest)
# 2. poptotal 변수를 total로 popasian 변수를 asian으로 수정
midwest_new = midwest
midwest_new = rename(midwst_new, total=poptotal, asian=popasian)
midwest_new = rename(midwest_new, total=poptotal, asian=popasian)
names((midwest_new))
# 3-1 새로운 변수만들기
midwest_new$per_asian = midwest_new$asian/midwest_new$total
midwest_new$per_asian
hist(midwest_new$per_asian)
hist(midwest_new$counry)
qplot(midwest_new$counry)
midwest$county
qplot(midwest_new$state)
# 3-2 도시 분포 보기
table(midwest_new$state)
# 4. 아시아 인구 전체 평균을 구하고 이를 기준으로 파생변수 만들기
# 4-1 아시아 비율 전체 평균 구하기
rate_per_asian = mean(midwest_new$per_asian)
rate_per_asian
midwest_new$grade_per_asian = ifelse(midwest_new$per_asian > rate_per_asian, "large", 'small')
midwest_new$grade_per_asian
hist(midwest_new$grade_per_asian)
qplot(midwest_new$grade_per_asian)
# 4. 아시아 인구 전체 평균을 구하고 이를 기준으로 파생변수 만들기
# 4-1 아시아 비율 전체 평균 구하기
ratio_per_asian = mean(midwest_new$per_asian)
ratio_per_asian
midwest_new$grade_per_asian = ifelse(midwest_new$per_asian > ratio_per_asian, "large", 'small')
midwest_new$grade_per_asian
# 3-1 새로운 변수만들기
midwest_new$per_asian = (midwest_new$asian/midwest_new$total) *100
midwest_new$per_asian
# 3-2 도시 분포 보기
table(midwest_new$state)
qplot(midwest_new$state)
##
hist(midwest_new$per_asian)
# 4. 아시아 인구 전체 평균을 구하고 이를 기준으로 파생변수 만들기
# 4-1 아시아 비율 전체 평균 구하기
mean_per_asian = mean(midwest_new$per_asian)
mean_per_asian
midwest_new$grade_per_asian = ifelse(midwest_new$per_asian > mean_per_asian, "large", 'small')
midwest_new$grade_per_asian
# 5. 4의 결과물을 히스토그램으로 보기
qplot(midwest_new$grade_per_asian)
